tags:
  - Student
summary: "🔏 Required role: STUDENT | Add subjects to the logged-in student"
description: Allows a student to add subjects to their profile.
security:
  - BearerAuth: []
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: array
      items:
        type: object
        required:
          - id
          - name
        properties:
          id:
            type: integer
            example: 1
          name:
            type: string
            example: "IT"
responses:
  201:
    description: Subjects added successfully
  400:
    description: Invalid input format
  404:
    description: Subject not found
  401:
    description: Unauthorized (missing or invalid token)
