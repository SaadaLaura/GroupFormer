tags:
  - Announcement
summary: Get all announcements
description: Retrieve all announcements in the system.
security:
  - BearerAuth: []
responses:
  200:
    description: List of all announcements
    schema:
      type: array
      items:
        $ref: '#/definitions/AnnouncementDTO'
  401:
    description: Unauthorized (missing or invalid token)
