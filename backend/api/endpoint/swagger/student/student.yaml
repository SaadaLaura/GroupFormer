tags:
  - Student
summary: "Liste tout les étudiants"
produces:
  - "application/json"
definitions:
  User:
    type: object
    properties:
      id_user:
        type: integer
      firstname:
        type: string
      lastname:
        type: string
      email:
        type: string
      password:
        type: string
responses:
  200:
    description: "OK"
    schema:
      type: array
      items:
        $ref: "#/definitions/User"
  404:
    description: "Not Found"
  500:
    description: "Internal Server Error"
